EDITOR="emacs -nw"

fuzzy_history() {
    history | awk '{for (i=2; i<NF; i++) printf $i " "; print $NF}' | fzf
}

disassemble() {
        objdump --disassemble="$2" -M intel --visualize-jumps=extended-color --no-show-raw-insn --no-addresses -w "$1" \
            | grep -v -E 'Disassembly of section|file format' | grep -v -E '^\s*$' | c++filt

}

fuzzy_disassemble() {
    # select symbols using objdump and fzf
    symbols=$(objdump -w --syms "$1" | grep -E '^[0-9]' | awk '{print $(NF-3), $(NF-2), $NF}' | grep -v -E '*UND*' | grep -E '^F' | awk '{print $NF}' | fzf -m 10 --bind 'ctrl-a:select-all' --query "$2")
    for symbol in $symbols
    do
        # disassemble each symbol
        disassemble $1 $symbol
        printf '\n'
    done
}


# determine if shell is pseudoterminal
is_pseudoterminal() {
    ls -l /proc/$$/fd | grep pts > /dev/null
}

SHOW_PWD='no'

toggle_pwd() {
    if [ $SHOW_PWD == no ]
    then
        SHOW_PWD=yes
    else
        SHOW_PWD=no
    fi
}

prompt_command() {
    if [ $SHOW_PWD == yes ]
    then
        echo -ne "\033]0;$(whoami)@$(hostname):$(pwd)\007"
    else
        echo -ne "\033]0;$(whoami)@$(hostname)\007"
    fi
}

prompts=(🍏 🍎 🍐 🍊 🍋 🍌 🍉 🍇 🍓 🫐 🍈 🍒 🥭 🍍 🥥 🥝 🍅 🍆 🥑 🥦 🥬 🥒 🫑 🌽 🥕 🫒 🧄)

if is_pseudoterminal
then
    size=${#prompts[@]}
    index=$(($RANDOM % $size))
    prompt=${prompts[$index]}
    PROMPT_COMMAND='prompt_command'
    PS1="${prompt} "
fi

eval "$(direnv hook bash)"
